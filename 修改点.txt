1.内挂:
SetupNeiguaItem 类中 __proto.update函数中 if(isOpen) 改成if(true)
SetupNeiguaView if(FunctionManager.isFunctionOpen(105)){ 去掉if判断

2.羊了哥羊:
YangGame 
window.setTimeout(() => { 
				this._view.showSuccess(true);
				console.log("成功通关，游戏结束");
				this.end();
			}, 300 * 1000)


3.修改攻击结果回调
位置: __proto.onS2C_AttackResultMessage=function(cmd){
添加: SamiraFight.kuafuBossHpAttackResultMessage(cmd);

4.最后添加samirafight
